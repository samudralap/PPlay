<apex:page standardController="Account">
        <apex:pageBlock >
            <apex:pageBlockTable value="{!account.contacts}" var="contact" id="Contactlist">
                <apex:column headerValue="First Name">
                <apex:outputText value="{!contact.firstname}"/>
                </apex:column>
            </apex:pageBlockTable>
        </apex:pageBlock>
</apex:page>